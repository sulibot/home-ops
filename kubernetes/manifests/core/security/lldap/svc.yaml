# basic/svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: lldap
  namespace: lldap
spec:
  type: ClusterIP
  selector:
    app: lldap
  ports:
    - name: http
      port: 80
        targetPort: http
    - name: ldap
      port: 389
        targetPort: ldap
