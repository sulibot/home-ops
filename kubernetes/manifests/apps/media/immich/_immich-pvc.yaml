apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: immich
  name: immich-pvc
  #current cephfs-pvc-media
  namespace: immich
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 60Ti
  storageClassName: cephfs-content-sc-retain
#  volumeMode: Filesystem
#  volumeName: cephfs-pv-media-ns-media

