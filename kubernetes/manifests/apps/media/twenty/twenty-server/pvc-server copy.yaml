apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: twentycrm-server-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: cephfs-content-sc-retain
