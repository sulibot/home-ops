apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: media
  labels:
    app: twenty
  name: twentycrm-server-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: cephfs-csi-sc-retain

