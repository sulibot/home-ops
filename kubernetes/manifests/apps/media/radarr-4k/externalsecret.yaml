apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: radarr-4k
  namespace: media
spec:
  dataFrom:
  - extract:
      conversionStrategy: Default
      decodingStrategy: None
      key: radarr-4k
      metadataPolicy: None
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    creationPolicy: Owner
    deletionPolicy: Retain
    name: radarr-4k-secret
    template:
      data:
        RADARR_4K__AUTH__APIKEY: '{{ .RADARR_4K_API_KEY }}'
      engineVersion: v2
      mergePolicy: Replace
