apiVersion: v1
kind: Node
metadata:
  labels:
    node-role.kubernetes.io/worker: "true"
  annotations:
    kubectl.kubernetes.io/preserves-pruning: "true"
spec:
  labels:
    custom-label.example.com/accelerator: "enabled"
