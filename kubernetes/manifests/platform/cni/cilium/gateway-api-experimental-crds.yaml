---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: backendlbpolicies.gateway.networking.k8s.io
spec:
  group: gateway.networking.k8s.io
  names:
    categories:
    - gateway-api
    kind: BackendLBPolicy
    listKind: BackendLBPolicyList
    plural: backendlbpolicies
    singular: backendlbpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BackendLBPolicy provides a way to configure load balancing behavior for a backend.

          Support for this policy is experimental.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of BackendLBPolicy.
            properties:
              sessionPersistence:
                description: |-
                  SessionPersistence defines and configures session persistence for the backend.

                  Support for this field is experimental.
                properties:
                  type:
                    description: |-
                      Type defines the type of session persistence.
                      A constant value for the field.
                    enum:
                    - IP
                    type: string
                required:
                - type
                type: object
              targetRef:
                description: |-
                  TargetRef identifies an API object to apply policy to.
                  Currently, the only supported TargetRef is a Service.
                  When this policy is targeting a Service, the policy is applicable to all instances of that Service, i.e. all Pods that are part of that Service.

                  Support for this field is experimental.
                properties:
                  group:
                    description: |-
                      Group is the group of the target resource.
                      Support: Core
                    maxLength: 253
                    pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                  kind:
                    description: |-
                      Kind is kind of the target resource.
                      Support: Service
                    maxLength: 63
                    pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                    type: string
                  name:
                    description: Name is the name of the target resource.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - targetRef
            type: object
        type: object
    served: true
    storage: true