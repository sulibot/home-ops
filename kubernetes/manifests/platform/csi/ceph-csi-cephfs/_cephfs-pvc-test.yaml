apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-test
  namespace: media
spec:
  storageClassName: csi-cephfs-data-content-sc-retain
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 40Ti
  volumeName: pv-test
