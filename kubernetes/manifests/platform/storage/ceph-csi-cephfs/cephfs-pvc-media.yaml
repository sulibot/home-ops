apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cephfs-pvc-media
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 40Ti
  volumeName: cephfs-pv-media
