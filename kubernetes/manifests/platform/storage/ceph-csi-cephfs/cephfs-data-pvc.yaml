apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cephfs-data-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 40Ti
  volumeName: data-cephfs-pv
