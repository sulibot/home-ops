---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# This is a placeholder component for alerts
# Can be extended later with PrometheusRule resources

resources: []
