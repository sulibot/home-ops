---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ./externalsecret.yaml
  - ./kopia-repository-pvc.yaml
  # Fallback PVC - Creates empty PVC if ReplicationDestination restore fails
  # Allows apps to start with empty storage when no backup exists
  # Apps requiring strict restore can override with pvc-restore.yaml
  - ./pvc.yaml
  - ./replicationdestination.yaml
  - ./replicationsource.yaml
