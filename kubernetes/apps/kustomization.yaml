---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Order matters: namespaces must exist before Kustomizations that reference them
  - ./cert-manager
  - ./external-secrets
  - ./ceph-csi
  - ./kube-system
