---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kopia-config
  namespace: data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-cephfs-config-sc
  resources:
    requests:
      storage: 1Gi
