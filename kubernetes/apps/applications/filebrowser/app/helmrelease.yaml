apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: filebrowser
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: filebrowser
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      filebrowser:
        type: deployment
        replicas: 1
        containers:
          app:
            image:
              repository: docker.io/gtstef/filebrowser
              tag: 1.2.0-experimental-sql-indexing-slim@sha256:b2d6defb13a8e625a88a8f0b03973ba2dc136d90d9dc09902d776a6f9f6e1732
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
            env:
              - name: TZ
                value: America/Los_Angeles
              - name: FB_ROOT
                value: /data
              - name: FB_DATABASE
                value: /config/filebrowser.db
              - name: FB_ADDRESS
                value: "::"          # bind v4/v6
              - name: FB_PORT
                value: "8080"
              - name: FB_NOAUTH
                value: "true"        # disable auth, credentials available via secret
              - name: FB_LOG
                value: stdout
              - name: FB_UID
                value: "1000"
              - name: FB_GID
                value: "1000"
            envFrom:
              - secretRef:
                  name: "{{ .Release.Name }}-secret"
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        controller: filebrowser
        ipFamilyPolicy: PreferDualStack
        ipFamilies:
          - IPv6
          - IPv4
        ports:
          http:
            port: 80
            targetPort: 8080
    route:
      app:
        hostnames:
          - filebrowser.sulibot.com
        parentRefs:
          - name: gateway-internal
            namespace: network
        rules:
          - backendRefs:
              - name: filebrowser
                port: 80
    persistence:
      config:
        enabled: true
        existingClaim: "{{ .Release.Name }}-config"
        globalMounts:
          - path: /config
      content:
        enabled: true
        type: persistentVolumeClaim
        existingClaim: csi-cephfs-content-pvc
        globalMounts:
          - path: /data
            subPath: data
      run:
        type: emptyDir
        medium: Memory
        globalMounts:
          - path: /run
      tmp:
        type: emptyDir
        medium: Memory
        globalMounts:
          - path: /tmp
