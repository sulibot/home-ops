---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-config
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: csi-cephfs-config-sc
