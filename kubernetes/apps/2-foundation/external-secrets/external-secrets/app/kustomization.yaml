---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../namespace.yaml
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./pdb.yaml
  # TODO: Add back grafanadashboard.yaml after Grafana operator is deployed
  # - ./grafanadashboard.yaml
