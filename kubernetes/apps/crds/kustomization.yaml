---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./gateway-api-crds/ks.yaml
  # snapshot-controller-crds moved to ceph-csi kustomization
