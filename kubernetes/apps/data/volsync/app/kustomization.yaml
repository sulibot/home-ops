---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../namespace.yaml
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  # - ./mutatingadmissionpolicy-jitter.yaml  # DISABLED: Causes pods to hang in PodInitializing
  # - ./mutatingadmissionpolicy-restore.yaml  # DISABLED: Causes duplicate kopia PVC mounts (volsync already has repository-pvc)
  # Note: grafanadashboard and prometheusrule removed to break circular dependency with kube-prometheus-stack
