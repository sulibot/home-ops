---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  # Legacy CephFS repository resources are retained for recovery/audit only.
  # The console reads the active MinIO repository and does not mount these PVCs.
  - ./kopia-config-pvc.yaml
  - ./kopia-repository-pv.yaml
  - ./kopia-repository-pvc.yaml
  - ./kopia-repository-subvolume-secret.yaml
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./vpa.yaml
