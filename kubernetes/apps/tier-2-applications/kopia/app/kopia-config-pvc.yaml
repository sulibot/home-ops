---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kopia-config
  namespace: volsync-system
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: csi-cephfs-config-sc
  resources:
    requests:
      storage: 1Gi
