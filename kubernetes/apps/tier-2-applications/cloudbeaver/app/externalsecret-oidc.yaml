---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudbeaver-oidc
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: cloudbeaver-oidc
    template:
      data:
        CLOUDBEAVER_OIDC_CLIENT_ID: "{{ .CLOUDBEAVER_OIDC_CLIENT_ID }}"
        CLOUDBEAVER_OIDC_CLIENT_SECRET: "{{ .CLOUDBEAVER_OIDC_CLIENT_SECRET }}"
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: authentik
