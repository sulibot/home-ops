---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: firefly-config
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: csi-cephfs-config-sc
