---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: filebrowser-oidc
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: filebrowser-oidc-config
    creationPolicy: Owner
    template:
      data:
        # Rendered to a Secret and copied into the writable /config PVC by the
        # filebrowser init container on first boot. This keeps the app config declarative
        # while allowing runtime writes (DB/cache/settings stay on the PVC).
        config.yaml: |
          server:
            database: /config/database.db
            sources:
              - path: /data/media
                name: photos
          auth:
            methods:
              password:
                enabled: false
              oidc:
                enabled: true
                clientId: "{{ .FILEBROWSER_OIDC_CLIENT_ID }}"
                clientSecret: "{{ .FILEBROWSER_OIDC_CLIENT_SECRET }}"
                issuerUrl: "https://auth.sulibot.com/application/o/filebrowser/"
                userIdentifier: "email"
                createUser: true
  dataFrom:
    - extract:
        key: authentik
