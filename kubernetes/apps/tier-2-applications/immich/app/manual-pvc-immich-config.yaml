---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-config
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 15Gi
  storageClassName: csi-cephfs-config-sc
