---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: baserow-config
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: csi-cephfs-config-sc
