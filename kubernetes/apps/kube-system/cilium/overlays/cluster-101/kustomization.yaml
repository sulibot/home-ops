---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system

# Cluster-101 (production) uses the base Cilium configuration directly
# This overlay exists for consistency with cluster-102 structure
resources:
  - ../../app
