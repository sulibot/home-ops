---
# Legacy directory - apps moved to layer-based structure
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
components:
  - ../../components/alerts
resources:
  - ./namespace.yaml
  # snapshot-controller namespace files remain here
