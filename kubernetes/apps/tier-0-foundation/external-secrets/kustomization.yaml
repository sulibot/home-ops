---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./namespace.yaml
  - ./external-secrets/ks.yaml
  - ./onepassword/ks.yaml
  # external-secrets-dashboard is intentionally excluded from Tier 0:
  # it depends on grafana-instance (Tier 1) and must not create cross-tier dependencies.
