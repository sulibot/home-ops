---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/batch/v1/cronjob.json
apiVersion: batch/v1
kind: CronJob
metadata:
  name: rbd-mount-doctor
  namespace: ceph-csi
spec:
  schedule: "*/10 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  jobTemplate:
    spec:
      activeDeadlineSeconds: 300
      backoffLimit: 0
      template:
        metadata:
          labels:
            app: rbd-mount-doctor
        spec:
          restartPolicy: Never
          serviceAccountName: rbd-mount-doctor
          containers:
            - name: mount-doctor
              image: alpine/k8s:1.34.1
              command: ["bash", "/scripts/rbd-mount-doctor.sh"]
              env:
                - name: AUTO_FIX
                  value: "true"
              volumeMounts:
                - name: script
                  mountPath: /scripts
              resources:
                requests:
                  cpu: 10m
                  memory: 64Mi
                limits:
                  memory: 256Mi
          volumes:
            - name: script
              configMap:
                name: rbd-mount-doctor
                defaultMode: 0o555
