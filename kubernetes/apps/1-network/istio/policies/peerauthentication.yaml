---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/security.istio.io/peerauthentication_v1beta1.json
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio-system
spec:
  mtls:
    mode: PERMISSIVE  # Allow both mTLS and plaintext (safe for gradual migration)
