---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../namespace.yaml
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  # - ./mutatingadmissionpolicy-jitter.yaml  # DISABLED: Causes pods to hang in PodInitializing
  - ./mutatingadmissionpolicy-restore.yaml  # ENABLED: Matches restore and backup jobs to mount shared repository PVC
  # Note: grafanadashboard and prometheusrule removed to break circular dependency with kube-prometheus-stack
