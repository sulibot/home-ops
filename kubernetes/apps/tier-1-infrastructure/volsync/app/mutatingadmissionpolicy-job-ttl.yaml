---
# Bound VolSync mover Jobs to 2 hours and clean their records 2 hours later.
# The execution deadline prevents a hung mover from traversing CephFS forever;
# ReplicationSource status and alerts retain the failure signal after cleanup.
apiVersion: admissionregistration.k8s.io/v1beta1
kind: MutatingAdmissionPolicyBinding
metadata:
  name: volsync-mover-job-ttl
spec:
  policyName: volsync-mover-job-ttl
---
apiVersion: admissionregistration.k8s.io/v1beta1
kind: MutatingAdmissionPolicy
metadata:
  name: volsync-mover-job-ttl
spec:
  matchConstraints:
    resourceRules:
      - apiGroups:
          - batch
        apiVersions:
          - v1
        operations:
          - CREATE
        resources:
          - jobs
  matchConditions:
    - name: is-volsync-mover-job
      expression: >-
        object.metadata.name.startsWith("volsync-dst-") || object.metadata.name.startsWith("volsync-src-")
    - name: has-volsync-created-by-labels
      expression: >-
        object.metadata.?labels["app.kubernetes.io/created-by"].orValue("") == "volsync"
    - name: ttl-not-already-set
      expression: >-
        !has(object.spec.ttlSecondsAfterFinished)
  failurePolicy: Ignore
  reinvocationPolicy: IfNeeded
  mutations:
    - patchType: JSONPatch
      jsonPatch:
        expression: >-
          [
            JSONPatch{
              op: "add",
              path: "/spec/ttlSecondsAfterFinished",
              value: 7200
            },
            JSONPatch{
              op: "add",
              path: "/spec/activeDeadlineSeconds",
              value: 7200
            }
          ]
