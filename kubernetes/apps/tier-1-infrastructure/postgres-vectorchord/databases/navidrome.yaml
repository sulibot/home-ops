apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: navidrome
  namespace: default
spec:
  cluster:
    name: postgres-vectorchord
  name: navidrome
  owner: navidrome
  ensure: present
