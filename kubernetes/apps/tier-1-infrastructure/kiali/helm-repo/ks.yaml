---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kiali-helm-repo
  namespace: flux-system
spec:
  interval: ${TIER1_INTERVAL:=1h}
  path: ./kubernetes/apps/tier-1-infrastructure/kiali/helm-repo
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
