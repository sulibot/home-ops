---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./alertmanagerconfig.yaml
  - ./externalsecret.yaml
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./vpa.yaml
  # PrometheusRule/ScrapeConfig resources live in rules/ because their CRDs
  # are installed by the kube-prometheus-stack HelmRelease.
