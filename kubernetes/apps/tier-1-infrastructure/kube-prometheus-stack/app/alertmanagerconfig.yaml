---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/alertmanagerconfig_v1alpha1.json
apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: alertmanager
  namespace: observability
spec:
  route:
    receiver: blackhole
    repeatInterval: 12h
    routes:
      - receiver: blackhole
        matchers:
          - name: alertname
            value: AlertmanagerFailedToSendAlerts|AlertmanagerClusterFailedToSendAlerts
            matchType: =~
      - receiver: blackhole
        matchers:
          - name: alertname
            value: InfoInhibitor
            matchType: =
      - receiver: buddy-heartbeat
        groupWait: 0s
        repeatInterval: 2m30s
        matchers:
          - name: alertname
            value: Watchdog
            matchType: =
      # Linear is the durable work log: always create/update a ticket for
      # actionable severities, regardless of time of day.
      - receiver: linear
        continue: true
        matchers:
          - name: severity
            value: warning|critical
            matchType: =~
      # Critical always interrupts, day or night.
      - receiver: pushover
        continue: true
        matchers:
          - name: severity
            value: critical
            matchType: =
      # Warning-level pages are muted overnight; the Linear ticket above still
      # captures them for review in the morning.
      - receiver: pushover
        continue: true
        muteTimeIntervals:
          - quiet-hours
        matchers:
          - name: severity
            value: warning
            matchType: =
      - receiver: email
        matchers:
          - name: severity
            value: critical
            matchType: =
    groupBy:
      - alertname
      - cluster
      - job
    groupInterval: 5m
    groupWait: 1m

  # NOTE: the installed AlertmanagerConfig CRD (prometheus-operator 0.89.0)
  # supports neither a `location`/timezone field nor a top-level `timeIntervals`
  # key - the field is `muteTimeIntervals`, and times are UTC-only. 21:00-08:00
  # America/Los_Angeles is currently UTC-7 (PDT) => 04:00-15:00 UTC, a single
  # non-wrapping range. With no weekday selector this applies every day. This
  # will drift by ~1h during PST (Nov-Mar) until prometheus-operator is upgraded
  # to a CRD version with `location` support.
  muteTimeIntervals:
    - name: quiet-hours
      timeIntervals:
        - times:
            - startTime: "04:00"
              endTime: "15:00"

  inhibitRules:
    - equal:
        - alertname
        - namespace
      sourceMatch:
        - name: severity
          value: critical
          matchType: =
      targetMatch:
        - name: severity
          value: warning
          matchType: =

  receivers:
    - name: blackhole
    - name: linear
      webhookConfigs:
        - url: http://alertmanager-linear-bridge.observability.svc.cluster.local:9096/webhook
          sendResolved: true
    - name: buddy-heartbeat
      webhookConfigs:
        - httpConfig:
            bearerTokenSecret:
              name: alertmanager-secret
              key: BUDDY_HEARTBEAT_TOKEN
          urlSecret:
            name: alertmanager-secret
            key: BUDDY_HEARTBEAT_URL
    - name: pushover
      pushoverConfigs:
        - html: true
          message: |-
            {{- range .Alerts }}
              {{- if ne .Annotations.description "" }}
                {{ .Annotations.description }}
              {{- else if ne .Annotations.summary "" }}
                {{ .Annotations.summary }}
              {{- else if ne .Annotations.message "" }}
                {{ .Annotations.message }}
              {{- else }}
                Alert description not available
              {{- end }}
              {{- if gt (len .Labels.SortedPairs) 0 }}
                <small>
                  {{- range .Labels.SortedPairs }}
                    <b>{{ .Name }}:</b> {{ .Value }}
                  {{- end }}
                </small>
              {{- end }}
            {{- end }}
          priority: |-
            {{ if eq .Status "firing" }}1{{ else }}0{{ end }}
          sendResolved: true
          sound: gamelan
          title: >-
            [{{ .Status | toUpper }}{{ if eq .Status "firing" }}:{{ .Alerts.Firing | len }}{{ end }}] {{ .CommonLabels.alertname }}
          ttl: 86400s
          token:
            name: alertmanager-secret
            key: ALERTMANAGER_PUSHOVER_TOKEN
          userKey:
            name: alertmanager-secret
            key: PUSHOVER_USER_KEY
          urlTitle: View in Alertmanager
    - name: email
      emailConfigs:
        - to: sahmad@sulibot.com,sulibot@sulibot.com
          from: noreply@sulibot.com
          smarthost: smtp-relay.default.svc.cluster.local:25
          requireTLS: false
          sendResolved: true
          text: |-
            {{ template "default.message" . }}
