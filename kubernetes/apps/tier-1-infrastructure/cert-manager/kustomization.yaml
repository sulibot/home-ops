---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helm-repo/helmrepository.yaml
  - app/helmrelease.yaml
  - app/externalsecret.yaml
  # issuers/ks.yaml is managed via tier-1-infrastructure/kustomization.yaml
  # (it needs postBuild variable substitution which cert-manager Kustomization lacks)
