---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helm-repo/helmrepository.yaml
  - app/helmrelease.yaml
  - app/externalsecret.yaml
  # Issuers deployed after cert-manager is ready (dependsOn cert-manager)
  - issuers/ks.yaml
