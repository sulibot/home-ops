---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./cert-manager/ks.yaml
  - ./cert-manager/issuers/ks.yaml
  - ./coredns/ks.yaml
  - ./descheduler/ks.yaml
  - ./intel-gpu-resource-driver/ks.yaml
  - ./metrics-server/ks.yaml
  - ./reloader/ks.yaml
  # Spegel disabled: libp2p P2P connections fail over IPv6 ULA with Cilium BPF
  # Error: "failed to dial: all dials failed [/ip6/fd00:101:224::*/tcp/5001] dial backoff/connection refused"
  # Alternative: DaemonSet-based image pre-pull (terraform/infra/modules/flux_instance/scripts/prepull-images.sh)

