---
- name: Reload network
  ansible.builtin.command: /usr/sbin/ifreload -a
  
- name: Restart per-vnet dnsmasq
  ansible.builtin.systemd:
    name: "dnsmasq-{{ item.name }}"
    state: restarted
  loop: "{{ tenant_vnets }}"
  when: "'pve' in group_names"
