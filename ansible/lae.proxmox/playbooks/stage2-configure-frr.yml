---
- name: Stage 2 - Configure FRR routing on Proxmox hosts
  hosts: pve
  become: true
  vars_files:
    - ../group_vars/all.yaml
    # - ../group_vars/cluster.yaml
    - group_vars/pve.yml
  roles:
    - frr
