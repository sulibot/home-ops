---
- name: Deploy FRR configuration
  template:
    src: frr.conf.j2
    dest: /etc/frr/frr.conf
  notify: restart frr

- name: Restart and enable FRR
  systemd:
    name: frr
    state: restarted
    enabled: true
