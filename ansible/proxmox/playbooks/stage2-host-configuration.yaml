- name: Stage 2 - Host configuration (chrony, sysctl, ssh, journald, fstrim)
  hosts: pve
  become: true
  vars_files:
  - "../group_vars/all.yaml"
  roles:
    - common
    - sysctl
    - ssh_config
    - journald
    - fstrim
