---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# namespace removed to avoid conflicts with PVCs in different namespaces
#components:
#  - ../../components/common
resources:
  - ./ceph-csi-cephfs/app/helm/namespace.yaml
  - ./ceph-csi-cephfs/ks.yaml
